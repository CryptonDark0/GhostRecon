rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      // Allow any authenticated agent to read/write tactical intel
      allow read, write: if request.auth != null;
    }
  }
}